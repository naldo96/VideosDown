services:
  bot:
    build: .
    container_name: telegram-video-bot
    restart: unless-stopped
    volumes:
      # Descargas en RAM: nada queda en disco del VPS
      - type: tmpfs
        target: /tmp
        tmpfs:
          size: 512m
